query GetCharacterDetail($characterId: ID!){
    character(id: $characterId){
        id
        name
        status
        species
        type
        gender
        origin {
           name
        }
        location {
            name
        }
        image
        episode {
            id
        }
        created
    }
}